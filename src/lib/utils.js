/**
 * lib/utils.js — cn() helper for conditional Tailwind class merging
 */
import { clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs) {
  return twMerge(clsx(inputs));
}
